openai>=2.48.0
python-dotenv>=1.2.2
customtkinter>=6.0.0
pillow>=12.0.0
sounddevice>=0.5.5
numpy>=2.0.0
pyttsx3>=2.99
pypdf>=6.15.0
pyinstaller>=6.21.0
# Optional: drag-and-drop onto the window. Without it the Attach
# button still works, so it is not required.
tkinterdnd2>=0.4.0
# 6.0: natural voices, Word and PDF documents, spreadsheets, PC status.
edge-tts>=7.2.0
python-docx>=1.2.0
fpdf2>=2.8.0
openpyxl>=3.1.0
psutil>=7.0.0
# 7.0: QR codes, and the time-zone database Windows' Python lacks.
qrcode>=8.0
tzdata>=2025.1
# 7.1: YouTube captions for /yt.
youtube-transcript-api>=1.2.0

# Optional: fully offline speech-to-text, no key and no network.
# pip install faster-whisper
